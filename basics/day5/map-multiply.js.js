//vytvořit nové pole, které obsahuje: každé číslo * 2

const numbers = [1, 3, 5, 7];
const result = []

for(let i = 0 ; i < numbers.length; i++){

        result.push(numbers[i] *2)
    
}
console.log(result)
